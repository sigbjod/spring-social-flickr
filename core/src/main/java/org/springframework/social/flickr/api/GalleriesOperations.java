package org.springframework.social.flickr.api;

public interface GalleriesOperations {

	// ? addPhoto
	// ? create
	// ? editMeta
	// ? editPhoto
	// ? editPhotos
	// ? getInfo
	// ? getList
	// ? getListForPhoto
	// ? getPhotos

}
